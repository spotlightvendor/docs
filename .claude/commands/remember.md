---
description: Recap prior conversations, key decisions, and current state of the help center
---

You are catching the user up on prior work in the SpotlightVendor help center repo so they can pick up where we left off.

Do this in order:

1. Read every file in `/Users/kathrynelishner/.claude/projects/-Users-kathrynelishner-Desktop-DEV-sv-docs/memory/` (including `MEMORY.md`).
2. Read `AGENTS.md` and `CLAUDE.md` at the repo root if they exist.
3. Run `git log --oneline -15` to see recent commits.
4. Read `docs.json` to understand the current sidebar structure.

Then produce a tight, scannable recap with these sections:

**Project context** — 2-3 bullets on what SpotlightVendor is, the ICP framework, and key product facts (pull from project memories).

**Conventions and feedback** — bulleted list of how the user prefers to work and any standing rules (pull from feedback memories).

**Current state of the help center** — what's live: audience sections, key pages, pricing approach, recent commits worth noting.

**Open items** — anything previously flagged as "next" or "pending" that hasn't shipped. If memories don't list any, say so explicitly.

Cap the recap at ~300 words. End with: "Ready to pick up where we left off — what do you want to work on?"

Do not editorialize, ask follow-up questions inside the recap, or add tasks. Just the recap.
